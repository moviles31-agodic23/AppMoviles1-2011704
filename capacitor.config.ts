import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AppMoviles1-2011704',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
